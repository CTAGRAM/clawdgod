# ──────────────────────────────────────────────────────────────
# ClawdGod Orchestrator — Environment Variables (Local Mode)
# Runs on the same host as the agents — no SSH needed.
# ──────────────────────────────────────────────────────────────

PORT=3002
NODE_ENV=production
LOG_LEVEL=info

# ── Backend Communication ──
# When running on the same host as docker-compose ClawdGod:
BACKEND_URL=http://localhost:8000
INTERNAL_API_SECRET=CHANGE_THIS_TO_MATCH_BACKEND
