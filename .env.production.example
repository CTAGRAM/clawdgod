# ──────────────────────────────────────────────────────────────
# ClawdGod — Production Environment Variables
# Copy this file to .env.production and fill in all values
# ──────────────────────────────────────────────────────────────

# ── Core ──
NODE_ENV=production
PORT=8000
FRONTEND_URL=https://YOUR_DOMAIN

# ── Database (Neon) ──
DATABASE_URL=postgresql://...

# ── Redis (Upstash) ──
UPSTASH_REDIS_URL=rediss://...

# ── Auth ──
JWT_SECRET=
MASTER_ENCRYPTION_KEY=
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# ── Ably (Realtime) ──
ABLY_API_KEY=

# ── Email (Resend) ──
RESEND_API_KEY=
FROM_EMAIL=hello@yourdomain.com

# ── Billing (Polar.sh) ──
POLAR_ACCESS_TOKEN=
POLAR_WEBHOOK_SECRET=
POLAR_STARTER_PRODUCT_ID=
POLAR_PRO_PRODUCT_ID=
POLAR_BUILDER_PRODUCT_ID=

# ── Orchestrator (optional — leave empty for standalone mode) ──
ORCHESTRATOR_URL=
INTERNAL_API_SECRET=

# ── Build-time vars (baked into frontend) ──
NEXT_PUBLIC_API_URL=
NEXT_PUBLIC_ABLY_KEY=
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
