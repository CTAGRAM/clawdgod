# ──────────────────────────────────────────────────────────────
# AgentDock Backend — Environment Variables
# Copy to .env and fill in your values
# ──────────────────────────────────────────────────────────────

# ── Server ──
PORT=3001
HOST=0.0.0.0
NODE_ENV=development
LOG_LEVEL=info
FRONTEND_URL=http://localhost:3000

# ── Database (Neon PostgreSQL) ──
# Sign up: https://neon.tech → Create project → Copy connection string
DATABASE_URL=postgresql://user:password@host.neon.tech/agentdock?sslmode=require

# ── Redis (Upstash) ──
# Sign up: https://upstash.com → Create database → Copy REDIS URL
UPSTASH_REDIS_URL=rediss://default:password@host.upstash.io:6379

# ── Auth ──
# Generate with: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=CHANGE_THIS_TO_A_RANDOM_64_BYTE_HEX_STRING

# ── Encryption (for API key storage) ──
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
MASTER_ENCRYPTION_KEY=CHANGE_THIS_TO_A_RANDOM_32_BYTE_HEX_STRING

# ── Anthropic (for personalization generation) ──
# Sign up: https://console.anthropic.com → Create API key
ANTHROPIC_API_KEY=sk-ant-...

# ── Ably (real-time push) ──
# Sign up: https://ably.com → Create app → Get API key
ABLY_API_KEY=your-ably-key

# ── Resend (transactional email) ──
# Sign up: https://resend.com → Create API key → Add domain
RESEND_API_KEY=re_...
FROM_EMAIL=hello@agentdock.io

# ── Polar.sh (billing) ──
# Already have: https://polar.sh → Settings → API tokens
POLAR_ACCESS_TOKEN=pat_...
POLAR_WEBHOOK_SECRET=whsec_...
POLAR_STARTER_PRODUCT_ID=prod_...
POLAR_PRO_PRODUCT_ID=prod_...
POLAR_BUILDER_PRODUCT_ID=prod_...

# ── Internal Service Communication ──
# Shared secret between backend and orchestrator
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
INTERNAL_API_SECRET=CHANGE_THIS_TO_A_RANDOM_SECRET
